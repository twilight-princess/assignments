import React from 'react'

function ContactList(props) {
    return (
        <div>
            {/* map through contacts and render contacts passing in props*/
                
            }
        </div>
    )
}

//connect to redux for the array of contacts
export default ContactList